#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(0); cin.tie(0);
	
	int N, K;
	cin >> N >> K;
	int S, Y;
	for(int i = 0; i < N; ++i){
		cin >> S >> Y;
		
	}
	
	
	return 0;
}